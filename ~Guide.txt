Android SDK
	Add Path
	Mac:
	nano ~/.zshrc
	export ANDROID_HOME=$HOME/Library/Android/sdk
	export PATH=$PATH:$ANDROID_HOME/platform-tools
	source ~/.zshrc

Appium
brew install node
node -v
npm -v

npm i -g appium
appium driver install uiautomator2
appium
Appium должен слушать http://127.0.0.1:4723.

curl -s http://127.0.0.1:4723/status


PyCharm
pip install appium-python-client pytest

PyCharm terminal:
adb devices

python test_appium_smoke.py

pip install pytest-bdd

check internet connection  adb shell ping -c 3 google.com

как узнать текущее активное приложение на телефоне
adb shell dumpsys window | grep -E "mCurrentFocus|mFocusedApp"

pip install allure-pytest
brew install allure

RUN with Allur Serv:
pytest
allure serve artifacts/allure-results


Git
git init
git add .
git commit -m "init: tests + github actions"
git branch -M main
git remote add origin https://github.com/<твой-логин>/<имя-репо>.git
git push -u origin main